import 'package:flutter/material.dart';

class AppDimensions {
  // default elevation
  static const double cardElevation = 8;

  // default corner radius
  static const double borderRadius = 8;
  static const double defaultBottomSheetRadius = 16;

  // default padding values for each side
  static const double paddingLeft = 16;
  static const double paddingRight = 16;
  static const double paddingTop = 24;
  static const double paddingBottom = 16;

  // default horizontal padding
  static const EdgeInsets horizontalPadding =
      EdgeInsets.symmetric(horizontal: 16);

  // default vertical padding
  static const EdgeInsets verticalPadding = EdgeInsets.symmetric(vertical: 16);

  // default horizontal margin
  static const EdgeInsets horizontalMargin =
      EdgeInsets.symmetric(horizontal: 8);

  // default vertical margin
  static const EdgeInsets verticalMargin = EdgeInsets.symmetric(vertical: 8);

    // default vertical list item margin
  static const EdgeInsets verticalListItemMargin = EdgeInsets.symmetric(vertical: 4);

  // default padding
  static const EdgeInsets defaultContentPadding =
      EdgeInsets.fromLTRB(16, 24, 16, 16);
}
